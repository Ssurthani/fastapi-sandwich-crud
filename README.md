# Sandwich Maker Machine - FastAPI CRUD

This project is a FastAPI-based CRUD application for managing sandwich data.

## Run Instructions
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
